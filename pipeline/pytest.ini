[pytest]
# Always look for tests in the tests/ directory
testpaths = tests

# Add common flags (e.g., verbose output)
addopts = -v

# Ensure your root directory is in the python path for easy imports
pythonpath = .